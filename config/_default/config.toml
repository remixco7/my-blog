baseurl      = "https://remixco.org/"
languageCode = "zh-cn"
title        = "Remixco's Lab"

# 核心：开启中文分词
hasCJKLanguage = true

# 核心：强制开启 JSON 输出（必须放在顶层）
[outputs]
    home = ["HTML", "RSS", "JSON"]
    page = ["HTML"]
    section = ["HTML"]

[pagination]
    pageSize = 5

# 评论系统（如果不用可以删掉）
disqusShortname = "hugo-theme-stack"
